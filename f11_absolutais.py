# Funkcija 11: math.fabs()
# Atgriež skaitļa absolūto vērtību (vienmēr pozitīvs)

import math

def piemers():
    skaitlis = -42.5
    rezultats = math.fabs(skaitlis)
    print(f"Absolūtā vērtība no {skaitlis} ir: {rezultats}")

piemers()
